<div class="body-container" data-controller="checklist-template">
  <div class="d-flex justify-content-between  checklist-template-phones">
    <div class="col-lg-6">
      <%= simple_form_for (@template) do |f| %>
        <div class="d-flex align-items-center">
          <h2 class="mb-3 me-5">NEW CHECKLIST TEMPLATE</h2>
          <%= f.submit "CREATE TEMPLATE", class:"button-green mb-3" %>
        </div>
        <div class="col-lg-5">
        <%= f.input :name,
            label: false,
            placeholder: "Template Name",
            input_html: { class: 'form-input' }  %>
        </div>


        <div data-checklist-template-target="descriptions">
          <div class="d-flex align-items-center">
            <%= render partial: 'description_fields', locals: { f: f } %>
          </div>
        </div>



      <a href="#" data-action="click->checklist-template#addDescription" class="button-green">ADD TASK</a>

        <%=f.input :concert, :as => :hidden, :input_html => { :value => @concert }  %>
        <%=f.input :tour, :as => :hidden, :input_html => { :value => @tour }  %>

      <% end %>
    </div>

  </div>
  <div class="container-tranparent col-lg-5">
      <div class="container-white">
        <p class="m-0"> <i class="fa-solid fa-circle-info me-3"></i> Templates are usefull for your tour management if you have repetitives tasks to check. The same template can be use in multiples tours. If you modify your template it will update on every tours and shows.</p>
      </div>
    </div>
</div>
