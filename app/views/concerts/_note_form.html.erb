
  <div class="mt-2">
    <%= simple_form_for [@tour, @concert, @note] do |f| %>
      <%= f.input :description, label: false, placeholder: "write a note", input_html: {class: 'form-input' }%>
      <%= f.submit "NEW NOTE", class:"button-green" %>
    <% end %>
  </div>
